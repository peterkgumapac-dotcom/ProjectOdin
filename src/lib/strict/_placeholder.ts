// Placeholder so `tsc -p tsconfig.strict.json` has at least one input until the
// first real strict-typed module lands. Delete this file once a real module is
// added under src/lib/strict/.
export const __strictPlaceholder = true
